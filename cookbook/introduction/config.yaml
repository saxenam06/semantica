api_keys:
  openai: your_openai_key_here  # Set via OPENAI_API_KEY environment variable
  anthropic: your_anthropic_key_here  # Set via ANTHROPIC_API_KEY environment variable
embedding:
  provider: openai
  model: text-embedding-3-large
  dimensions: 3072
knowledge_graph:
  backend: networkx
  temporal: true
